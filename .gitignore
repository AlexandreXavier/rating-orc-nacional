# Dependencies
node_modules/
.venv/

# Build output
dist/
build/
.svelte-kit/

# Reference material (kept local, not versioned). Os artefactos do cruzamento
# (src/generated/*.json) SÃO versionados: o build regenera-os quando LIXO/ está
# presente e usa os versionados quando não está (e.g. Vercel) — ver ADR 0001.
LIXO/

# OS
.DS_Store
*.local
