{
  "name": "rating-orc-nacional",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:provas": "node scripts/build-provas.mjs",
    "dev": "npm run build:provas && vite",
    "build": "npm run build:provas && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.1.2",
    "svelte": "^4.2.19",
    "vite": "^5.4.21"
  }
}
